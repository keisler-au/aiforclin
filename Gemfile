source "https://rubygems.org"

ruby "~> 3.3"   # optional but nice

gem "jekyll", "~> 4.4.1"

# Ruby 3.4 no longer auto-loads these; safe_yaml pulls them in.
gem "base64"
gem "logger"

group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag"
end
